<?xml version="1.0" encoding="utf-8"?>
<!-- Rebuilt to mirror item_provider_group_header.xml exactly (icon + title + count
     badge + collapse arrow + divider) so the Offline Models groups (Gemma/Qwen)
     look and behave the same as the AI Providers groups (Free/Paid) below. -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_marginTop="8dp"
    android:layout_marginBottom="4dp">

    <!-- Divider line above (hidden for first group) -->
    <View
        android:id="@+id/group_divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginBottom="12dp"
        android:background="?attr/colorOutlineVariant" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="4dp">

        <!-- Group icon -->
        <ImageView
            android:id="@+id/group_icon"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:scaleType="fitCenter"
            app:tint="?attr/colorPrimary" />

        <!-- Group title -->
        <TextView
            android:id="@+id/tv_group_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:textAppearance="?attr/textAppearanceLabelLarge"
            android:textStyle="bold"
            android:textColor="?attr/colorPrimary" />

        <!-- Model count badge -->
        <TextView
            android:id="@+id/group_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="8dp"
            android:paddingVertical="2dp"
            android:textSize="11sp"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:background="@drawable/bg_ai_get_key_button" />

        <!-- Collapse/expand arrow -->
        <ImageView
            android:id="@+id/group_arrow"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_marginStart="4dp"
            android:src="@drawable/ic_mtrl_arrow_down"
            android:rotation="0"
            app:tint="?attr/colorOnSurfaceVariant" />

    </LinearLayout>

    <!-- Group subtitle -->
    <TextView
        android:id="@+id/tv_group_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="8dp"
        android:paddingHorizontal="4dp"
        android:textAppearance="?attr/textAppearanceLabelSmall"
        android:textColor="?attr/colorOnSurfaceVariant" />

</LinearLayout>
