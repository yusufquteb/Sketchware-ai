<?xml version="1.0" encoding="utf-8"?>
<!--
    item_chat_msg_tool.xml — Stage 3 FINAL
    ========================================
    Tool result message card.
    Distinct from AI messages: uses colorTertiaryContainer, code-style font,
    monospace content area, and a "tool" badge chip.

    IDs referenced by ToolViewHolder:
      - chat_msg_tool_name     (TextView: tool name badge)
      - chat_msg_text          (TextView: tool output content)
      - chat_msg_btn_copy      (ImageButton)
      - chat_msg_time          (TextView: timestamp)
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="6dp"
    android:paddingBottom="2dp"
    android:paddingStart="12dp"
    android:paddingEnd="52dp"
    android:clickable="false"
    android:focusable="false"
    android:descendantFocusability="afterDescendants">

    <!-- Header: tool icon + tool name badge + timestamp -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="5dp"
        android:clickable="false"
        android:focusable="false">

        <!-- Tool icon circle -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginEnd="8dp"
            app:cardCornerRadius="14dp"
            app:cardElevation="0dp"
            app:cardBackgroundColor="?attr/colorTertiaryContainer"
            app:strokeWidth="0dp">

            <ImageView
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_chat_tool"
                android:contentDescription="@null"
                app:tint="?attr/colorOnTertiaryContainer" />
        </com.google.android.material.card.MaterialCardView>

        <!-- Tool name badge -->
        <TextView
            android:id="@+id/chat_msg_tool_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="3dp"
            android:paddingBottom="3dp"
            android:background="@drawable/bg_tool_badge"
            android:textAppearance="?attr/textAppearanceLabelSmall"
            android:textColor="?attr/colorOnTertiaryContainer"
            android:fontFamily="monospace" />

        <!-- Timestamp -->
        <TextView
            android:id="@+id/chat_msg_time"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textAppearance="?attr/textAppearanceLabelSmall"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:alpha="0.55" />

    </LinearLayout>

    <!-- Tool result card -->
    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="12dp"
        app:cardElevation="0dp"
        app:cardBackgroundColor="?attr/colorSurfaceContainerLowest"
        app:strokeColor="?attr/colorOutlineVariant"
        app:strokeWidth="1dp"
        android:clickable="false"
        android:focusable="false">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:clickable="false"
            android:focusable="false"
            android:descendantFocusability="afterDescendants">

            <!-- Tool output content (monospace for code/data) -->
            <TextView
                android:id="@+id/chat_msg_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingStart="14dp"
                android:paddingEnd="14dp"
                android:paddingTop="12dp"
                android:paddingBottom="10dp"
                android:lineSpacingMultiplier="1.4"
                android:textAppearance="?attr/textAppearanceBodySmall"
                android:textColor="?attr/colorOnSurface"
                android:fontFamily="monospace"
                android:textIsSelectable="true"
                android:longClickable="true"
                android:clickable="false"
                android:focusableInTouchMode="true"
                android:maxLines="12"
                android:ellipsize="end" />

            <!-- Footer actions -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingStart="4dp"
                android:clickable="false"
                android:focusable="false">

                <ImageButton
                    android:id="@+id/chat_msg_btn_copy"
                    android:layout_width="44dp"
                    android:layout_height="44dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="@string/chat_copy_message"
                    android:padding="12dp"
                    android:scaleType="fitCenter"
                    android:src="@drawable/ic_chat_copy"
                    app:tint="?attr/colorOnSurfaceVariant" />

            </LinearLayout>

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

</LinearLayout>
